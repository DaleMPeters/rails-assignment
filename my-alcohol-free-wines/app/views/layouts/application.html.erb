<%# Holds the definition of the layout of a general page in order to promote consistency %>
<!DOCTYPE html>
<html>
<head>
  <title>My Alcohol Free Wines</title>
  <%= stylesheet_link_tag "bootstrap.css" %>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="container">
  <p id="notice"><%= notice %></p>
  <header class="jumbotron">
    <h1>My Alcohol Free Wines</h1>
  </header>
  <div class="col-md-6">
    <p><%= link_to('Home', wines_path) %> |
    <% if customer_is_logged_in? %>
      <%= logged_in_customer.name %> | <%= link_to('Logout', logout_path, method: :delete) %> </p>
    <% else %>
      <%= link_to('Login', login_path) %> | <%= link_to('Register', new_customer_path) %></p>
    <% end %>
  </div>
  <div class="col-md-6"><span class="pull-right">
      <%= form_tag wines_path, method: "get" do %>
        <p>
          <%= text_field_tag :search_query, params[:search_query], placeholder: "Search Wines..." %>
          <%= submit_tag "Search", name: nil %>
        </p>
      <% end %>
    </span>
  </div>
  
  <%= yield %>
</div>
</body>
</html>
